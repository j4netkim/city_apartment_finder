<%= form_for [@apartment, @amenity_apartment] do |f| %>
    <br />
    
            <%= f.collection_select(:amenity, @apartment.available_amenities, :id, :name, {}, {class: 'browser-default'}) %>
            <%= f.label :amenity %>

    <br />

    <%= f.submit %>
<% end %>


